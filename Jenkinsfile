pipeline {
    agent any
    stages {
        stage('Testing Build') {
            steps {
                echo 'Starting Clone Stage'
                echo 'Build Finished'
            }
        }
    }
}
